import styled from "styled-components";

export const Container = styled.div`
  img {
    width: 20rem;
  }
`;

export const Info = styled.div`
  background-color: #fff;
  border-radius: 0.4ren;
  width: 16rem;
  padding: 0.8rem;
  margin: 8rem 0 20rem 0;
  box-shadow: 0.2rem 0.2 rem 0.4rem #000;
`;
